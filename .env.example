# Vikunja instance base URL (no trailing slash)
VIKUNJA_BASE_URL=https://vikunja.example.com

# API token created in Vikunja → Settings → API Tokens
VIKUNJA_API_TOKEN=your-api-token-here

# MCP server bind address and port (defaults shown)
MCP_HOST=0.0.0.0
MCP_PORT=8000
